import 'package:flutter/material.dart';

class ProjectDebtTab extends StatelessWidget {
  const ProjectDebtTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Placeholder());
  }
}
