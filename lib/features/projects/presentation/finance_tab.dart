import 'package:flutter/material.dart';

class ProjectFinanceTab extends StatelessWidget {
  const ProjectFinanceTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Placeholder());
  }
}
