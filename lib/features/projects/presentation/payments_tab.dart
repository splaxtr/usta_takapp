import 'package:flutter/material.dart';

class ProjectPaymentsTab extends StatelessWidget {
  const ProjectPaymentsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Placeholder());
  }
}
